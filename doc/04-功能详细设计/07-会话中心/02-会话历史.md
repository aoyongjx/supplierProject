# 会话历史

## 菜单与路由

- 路由：`/sessions, /sessions/:id`

## 功能说明

历史会话查看、置顶/重命名/删除。

## 关键接口

- GET /api/sessions
- GET /api/sessions/:id
- PATCH/DELETE /api/sessions/:id

## 业务规则

- 会话列表按更新时间刷新。

## 验收标准

1. 页面可正常加载，无白屏。
2. 关键操作返回成功或明确错误提示。
3. 数据变更可在页面刷新后保持一致。



