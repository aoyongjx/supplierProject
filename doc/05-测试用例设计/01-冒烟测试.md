# 冒烟测试

| 编号 | 场景 | 前置条件 | 步骤 | 预期结果 |
| --- | --- | --- | --- | --- |
| ST-01 | 服务启动 | `.env` 配置完成 | 执行 `npm run stable` | 前端 `5173`、后端 `3000` 正常监听 |
| ST-02 | 登录可用 | Auth 服务可用 | 访问系统并完成登录 | 成功进入首页，`/api/auth/me` 返回用户信息 |
| ST-03 | 关键列表页加载 | 已登录 | 进入供应商/供应链/知识库页面 | 页面无报错，列表正常渲染 |
| ST-04 | 核心 API 健康 | 后端启动 | 调用 `GET /api/health` | 返回成功与 DB 状态 |
| ST-05 | 配置页可访问 | 已登录 | 打开模型管理、数据源管理、MCP 服务 | 页面加载成功，无白屏 |
